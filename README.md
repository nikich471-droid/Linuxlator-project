# Linuxlator-project
Linuxlator — это приложение, которое позволяет запускать полноценный Linux-дистрибутив на Android без root, с поддержкой arm64 и x64 программ для linux и поддержкой GPU, аналогично Winlator, но для Linux.

приблизительная дата выхода: ??.0?.2026
